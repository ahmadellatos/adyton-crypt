PySide6>=6.6.0
PySideSix-Frameless-Window>=0.3.0
cryptography>=46.0.7,<47.0.0
QtAwesome>=1.3.0
loguru>=0.7.0
windows-toasts>=1.3.0
zxcvbn>=4.4.28
qrcode>=7.4.2                      # QR share untuk teks terenkripsi (tanpa Pillow — render via QPainter)

# =============================================================================
# DEV / QUALITY TOOLING (Phase 3+)
# =============================================================================
# Install with:
#   .\venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Recommended usage after install:
#   ruff check .                    # Lint
#   ruff format .                   # Format (Black-compatible)
#   bandit -r core/                 # Security scanning (very important for crypto)
#   pre-commit install              # (after creating .pre-commit-config.yaml)
#   pytest -m security              # Run security-related tests only
#
# =============================================================================

# --- Build ---
nuitka>=2.0.0                  # Compiler Python ke standalone .exe

# --- Testing ---
pytest>=8.0.0
pytest-qt>=4.4.0
coverage>=7.4.0

# --- Linting & Formatting (recommended stack) ---
ruff>=0.5.0                    # Extremely fast linter + formatter (replaces flake8, isort, pyupgrade, etc.)
bandit>=1.7.9                  # Security-focused linter (critical for cryptography code)
mypy>=1.10.0                   # Gradual static typing (optional but recommended long-term)
pip-audit>=2.7.0               # Scan installed packages for known vulnerabilities

# --- Git Hooks (recommended) ---
pre-commit>=3.7.0              # Framework to run hooks before every commit

# --- Notes for future improvement ---
# When ready, create .pre-commit-config.yaml with at least:
#   - ruff
#   - bandit
#   - trailing-whitespace
#   - end-of-file-fixer
#
# Example minimal ruff config (can be added to pyproject.toml later):
# [tool.ruff]
# line-length = 100
# target-version = "py310"
# [tool.ruff.lint]
# select = ["E", "F", "I", "UP", "B", "S"]   # S = bandit rules via ruff
# =============================================================================
